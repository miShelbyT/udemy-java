package com.company;

public class Main {

    public static void main(String[] args) {
	// write your code here
        //if a mile is equal to 1.609344 kilometers, then setting 100 miles in km would be
        double kilometers = (100 * 1.609344);
        // this whole part is the expression  => kilometers = (100 * 1.609344)
        // adding the datatype, in this case double, and the semicolon ; - this makes it a valid Java statement
        int highScore = 50;
        if(highScore == 50) {
            System.out.println("this is an expression");
        }
        //highScore = 50 is an expression, highScore == 50 inside the if (which is a control flow statement) is also an expression, as is "this is an expression"

        //score = 100 // score > 99 // "You got the high score!" // score = 0

    }
}
